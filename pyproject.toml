[tool.pytest.ini_options]
pythonpath = ["."]

[tool.mypy]
ignore_missing_imports = true
exclude = ["devel", "build", "dist"]

[tool.isort]
profile = "black"
line_length = 140

[tool.black]
line_length = 140
